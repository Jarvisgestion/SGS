# Conexión a la base (ver db/README.md para crearla)
DATABASE_URL=postgres://localhost/sgs_dev

# Clave de firma de los tokens de sesión. Generar con: openssl rand -hex 32
SGS_SESSION_SECRET=

PORT=3000
